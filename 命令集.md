# 3Axis_MARS 命令集

> 固件版本: v1.x | 更新日期: 2026-07-23

## 轴定义

| 轴 | 类型 | 说明 |
|----|------|------|
| **X** | 滑台线性轴 | 水平方向（探头贴靠/远离） |
| **Y** | 滑台线性轴 | 垂直方向（升降，v1.x新增） |
| **Z** | 滑台线性轴 | 前后方向 |
| **R** | 卡盘旋转电机 | 带动样品旋转（采集时圆周运动） |

## 运动控制

| 命令 | 功能 | 示例 | 成功响应 | 错误响应 |
|------|------|------|----------|----------|
| `X<steps>~` | X轴运动(带符号) | `X+1000~` `X-500~` | `X START 1000` | `X BUSY` `X RANGE` `X LIMIT` `X VBUS LOW` `X LIGHT TRIGGERED` `X ADC NOT READY` `X ERR` |
| `Y<steps>~` | Y轴运动(带符号) | `Y+1000~` `Y-500~` | `Y START 1000` | 同上(Y) |
| `Z<steps>~` | Z轴运动(带符号) | `Z+1000~` `Z-500~` | `Z START 1000` | 同上(Z) |
| `R<steps>~` | R轴(卡盘)运动 | `R+1000~` `R-500~` | `R START 1000` | 同上(R) |
| `RSPEED<start>,<target>~` | 卡盘转速设置 | `RSPEED200,1000~` | `R SPEED OK` | — |
| `RSPEED~` | 查询卡盘转速 | `RSPEED~` | `R SPEED: start=200 target=1000` | — |
| `O~` | 全部急停 | `O~` | `ALL STOP` | — |
| `XSTOP~` | 急停(O~别名) | `XSTOP~` | `ALL STOP` | — |
| `XE<0\|1>~` | X轴使能/禁用 | `XE0~` `XE1~` | `X DISABLED` / `X ENABLED` | — |
| `EN<0\|1>~` | X轴使能(XE别名) | `EN0~` `EN1~` | 同上 | — |

## 采集控制

| 命令 | 功能 | 示例 | 响应 |
|------|------|------|------|
| `B~` | 开始一圈采集 | `B~` | 成功: 无文本, 进入二进制流模式; 失败: `ADC FAIL` `ADC BUSY` `R SPEED TOO HIGH` |
| `S~` | 停止采集 | `S~` | 无文本 (始终静默) |
| `MODE<0\|1\|2>~` | 切换采集模式 | `MODE0~` | `MODE 0 OK (1200 Hz, div 1, 131072 edges, 131072 samples/rev)` |
| `MODE~` | 查询当前模式 | `MODE~` | 同上格式 |
| `D~` | 查询分频(只读) | `D~` | `SAMPLE_DIV: 1` |

## 归零

| 命令 | 功能 | 示例 | 响应 |
|------|------|------|------|
| `I~` | 三轴归零 (X→Y→Z) | `I~` | `HOME START` |

## 状态查询

| 命令 | 功能 | 示例 | 响应 |
|------|------|------|------|
| `M~` | 系统全状态 | `M~` | 多行文本 (ADC/轴状态/限位/故障等) |
| `A~` | 单次双通道ADC | `A~` | 4字节二进制: `[Hall_H][Hall_L][Curr_H][Curr_L]` |
| `TEST~` | 双通道电压(mV) | `TEST~` | `Hall: xxx raw  x.xxx mV` `Curr: xxx raw  x.xxx mV` |

## 其他

| 命令 | 功能 | 示例 | 响应 |
|------|------|------|------|
| `F<0\|1>~` | 风扇开关 | `F0~` `F1~` | `FAN OFF` / `FAN ON` |

## 采集模式速查

| 模式 | 分辨率(样本/圈) | 速度上限 | 数据量/圈 | 推荐场景 |
|------|-----------------|----------|-----------|----------|
| MODE0 | 131072 | 1200 Hz | 262144 B | 高分辨率旋转测量 |
| MODE1 | 65536 | 2000 Hz | 131072 B | 平衡模式 |
| MODE2 | 32768 | 3500 Hz | 65536 B | 高速垂直测量 |

## 运动完成判定

1. 发送运动命令后，监听 `{轴} DONE` 表示运动完成
2. 若收到 `{轴} BUSY`，需等待当前运动结束后重试
3. 若收到其他错误响应，需根据具体错误类型处理(如检查限位/电源/碰撞)

## 与旧版命令对照

| 旧命令 (V3.5) | 新命令 (v1.x) | 说明 |
|---------------|---------------|------|
| `X5000~` | `X+5000~` | 必须带符号 |
| `Z3000~` | `Z+3000~` | 必须带符号 |
| `P~` | — | 已移除(Z轴自检) |
| `Y~` / `Y-~` | — | 已移除(X轴自检) |
| `N<num>~` | `Z<num>~` | 统一为轴运动命令 |
| `?X~` / `?Z~` / `?XZ~` | `M~` | 通过M~查询位置 |
| `R~` | — | 已移除(MRAM读取) |
| `K<sec>~` | — | 已移除(定时采集) |
| `T~` | `TEST~` | 功能类似, 响应格式不同 |
| — | `Y<steps>~` | 新增滑台Y轴控制 |
| — | `R<steps>~` | 新增卡盘R轴控制 |
| — | `RSPEED~` | 新增卡盘转速配置 |
| — | `MODE~` | 新增采集模式切换 |
| — | `F~` | 新增风扇控制 |

> **注意**: 新版协议中滑台为 X/Y/Z 三轴（新增Y轴），R轴为卡盘旋转电机（替代旧版的直流有刷电机）。
