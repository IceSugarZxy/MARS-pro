# MARS 代码结构说明

本文档按当前 `src/` 源码整理项目结构、每个 Python 文件的职责、主要类与函数说明，并记录当前注释/docstring 覆盖情况。项目是基于 PyQt5 的旋转体表磁测量分析系统，核心流程包括串口通信、运动控制、偏置校准、测量采集、波形分析、历史数据与数据比对。

## 一、源码目录结构

```text
src/
├─main.py                         # 程序入口，创建 QApplication、主窗口和线程管理器
├─__init__.py                      # 包说明
├─core/                            # 核心业务与基础设施
│  ├─config_manager.py             # 配置文件读写和测试方案管理
│  ├─data_process.py               # 串口数据解析、测量数据处理、文件保存
│  ├─logger.py                     # 日志初始化和 logger 获取
│  ├─offset_calibration_config.py   # 偏置校准时长常量
│  ├─path_utils.py                 # 源码运行/打包运行路径工具
│  ├─serial_command.py             # 串口命令封装、运动流程和异步状态协调
│  ├─serial_manager.py             # QSerialPort 连接、读写队列和连接状态维护
│  ├─thread_manager.py             # 串口线程、数据处理线程和信号桥
│  └─__init__.py                   # core 包说明
├─ui/                              # 主题、窗口操作和 Qt Designer UI 文件
│  ├─theme.py                      # 应用样式表
│  └─window_operations.py          # 自定义窗口拖拽、最小化、最大化、关闭
└─windows/                         # 主窗口、面板、弹窗和绘图/分析界面
   ├─analysis_detail_dialog.py     # 波形分析详情弹窗
   ├─compare_panel.py              # 数据比对面板
   ├─history_panel.py              # 历史数据面板
   ├─main_panel.py                 # 主窗口壳和页面导航
   ├─measure_panel.py              # 测量主面板
   ├─measure_type_dialog.py        # 测量类型选择弹窗
   ├─offset_calibration_dialog.py  # 偏置校准进度弹窗
   ├─plot_window.py                # pyqtgraph 绘图窗口封装
   ├─scheme_edit_dialog.py         # 运动方案编辑弹窗
   ├─test_config_panel.py          # 测试配置面板
   ├─test_progress_dialog.py       # 测试进度弹窗
   ├─wave_analysis.py              # 波形分析算法
   └─__init__.py                   # windows 包说明
```

## 二、整体调用关系

1. `main.py` 创建 `MainApplication`，初始化 `ThreadManager` 和 `MainPanel`。
2. `ThreadManager` 创建串口管理器 `SerialManager`、命令封装 `SerialCommand`、数据处理器 `DataProcess`，并用 Qt 信号连接 UI 与后台线程。
3. `MainPanel` 加载测量、测试配置、历史数据、数据比对等面板，负责页面切换和底部状态栏。
4. `MeasurePanel` 与 `TestConfigPanel` 共享串口连接状态和测试配置，串口连接/断开由同一个 `btnSuccess` 按钮控制。
5. `SerialManager` 负责真实串口连接、读取和写入；`SerialCommand` 负责把 UI 操作转换成设备命令和运动流程。
6. `DataProcess` 解析采集数据并保存 CSV；`WaveAnalysis` 对波形执行极值、过零点、周期误差和 THD 等分析。
7. `HistoryPanel` 读取历史 CSV 并回填到测量页；`ComparePanel` 读取两个历史文件进行曲线和结果比对。

## 三、注释/docstring 覆盖情况

当前源码大部分文件已有模块级说明，许多用户可见槽函数也已有中文 docstring。仍有一些内部辅助函数、属性 getter/setter、事件处理函数没有逐个写 docstring，这些函数多为命名清晰的私有方法或 PyQt 生命周期方法。本文档已为这些函数补充用途说明，便于后续维护。

已完成的结构检查结果：

- `src/` 下共 26 个 Python 文件。
- 未发现高置信未引用的私有方法。
- 未发现高置信只赋值不读取的私有成员变量。
- 已移除旧的无调用函数后，`python -m compileall -q src` 通过。

## 四、逐文件说明

### `src/__init__.py`

文件作用：声明 MARS 源码包，提供包级说明。

函数/类说明：无运行逻辑。

### `src/main.py`

文件作用：程序入口，负责创建 Qt 应用、初始化主窗口、线程管理器和退出清理。

类/函数说明：

- `MainApplication`：主应用程序协调类，封装应用生命周期。
- `MainApplication.__init__()`：初始化应用对象引用、线程管理器和主窗口占位。
- `MainApplication.initialize()`：创建 `QApplication`、初始化日志、加载 `ThreadManager` 和 `MainPanel`。
- `MainApplication.run()`：启动应用并进入 Qt 事件循环。
- `MainApplication._run_event_loop()`：执行 `QApplication.exec_()`。
- `MainApplication.cleanup()`：退出时停止线程、关闭资源。
- `main()`：命令行入口函数，创建并运行 `MainApplication`。

### `src/core/__init__.py`

文件作用：声明核心模块包。

函数/类说明：无运行逻辑。

### `src/core/config_manager.py`

文件作用：集中管理 `configuration.txt` 配置读写、测试模式、探头量程、运动方案和位移换算。

类/函数说明：

- `action_to_text(action)`：把运动步骤动作标识转换为界面显示文本。
- `text_to_action(text)`：把界面显示文本转换为内部动作标识。
- `ConfigManager`：配置管理器，负责配置文件加载、保存和字段访问。
- `ConfigManager.__init__(config_file)`：设置配置文件路径并加载默认配置。
- `ConfigManager.load()`：从配置文件读取键值配置。
- `ConfigManager.save()`：保存当前配置到文件。
- `ConfigManager.get(key, default)`：读取字符串配置。
- `ConfigManager._strip_quotes(value)`：去除配置值外层引号。
- `ConfigManager.set(key, value)`：更新配置并写回文件。
- `ConfigManager.get_int(key, default)`：读取整数配置。
- `ConfigManager.get_float(key, default)`：读取浮点配置。
- `ConfigManager.com_port`：串口号配置属性。
- `ConfigManager.offset`：偏置值配置属性。
- `ConfigManager.test_x` / `test_z`：测试位置 X/Z 坐标配置属性。
- `ConfigManager.suspend_x` / `suspend_z`：挂起位置 X/Z 坐标配置属性。
- `ConfigManager.test_type`：测试类型配置属性。
- `ConfigManager.test_speed`：测试速度配置属性。
- `ConfigManager.sensor_range`：探头量程配置属性。
- `ConfigManager.test_movement_scheme`：当前测试位置移动方案配置属性。
- `ConfigManager.suspend_movement_scheme`：当前挂起位置移动方案配置属性。
- `ConfigManager.inner_x_offset` / `inner_z_offset`：内侧测试 X/Z 偏移量配置属性。
- `ConfigManager.retract_distance`：贴靠后回弹距离配置属性。
- `ConfigManager._empty_type_scheme()`：生成空的测试类型方案结构。
- `ConfigManager._build_default_type_scheme(test_type, schemes)`：为测试类型构建默认运动方案。
- `ConfigManager._normalize_test_type_schemes(schemes)`：规范化配置中的多测试类型方案结构。
- `ConfigManager._get_schemes()`：读取所有移动方案配置。
- `ConfigManager._save_schemes(schemes)`：保存所有移动方案配置。
- `ConfigManager.get_test_type_schemes()`：获取全部测试类型的方案。
- `ConfigManager.get_schemes_for_type(test_type)`：获取指定测试类型的方案集合。
- `ConfigManager.get_test_schemes(test_type)`：获取指定类型的测试位置方案列表。
- `ConfigManager.get_suspend_schemes(test_type)`：获取指定类型的挂起位置方案列表。
- `ConfigManager.get_active_test_scheme_index(test_type)`：读取当前测试方案索引。
- `ConfigManager.get_active_suspend_scheme_index(test_type)`：读取当前挂起方案索引。
- `ConfigManager.get_active_test_scheme(test_type)`：读取当前测试位置方案。
- `ConfigManager.get_active_suspend_scheme(test_type)`：读取当前挂起位置方案。
- `ConfigManager.set_active_scheme_index(test_type, is_test, index)`：设置当前选中的方案索引。
- `ConfigManager.update_scheme(test_type, is_test, scheme_index, scheme)`：更新指定方案。
- `ConfigManager.add_scheme(test_type, is_test, scheme)`：新增运动方案。
- `ConfigManager.delete_scheme(test_type, is_test, scheme_index)`：删除运动方案。
- `ConfigManager.get_inner_x_offset_pulse()`：将 X 偏移量按标定系数换算为脉冲。
- `ConfigManager.get_inner_z_offset_pulse()`：将 Z 偏移量按标定系数换算为脉冲。
- `get_config_manager()`：获取全局单例配置管理器。

### `src/core/data_process.py`

文件作用：处理串口接收队列中的自检、位置、测量和偏置数据，执行测量数据截取、滤波、保存和结果信号发送。

类/函数说明：

- `DataProcess`：数据处理器，运行在数据处理线程中。
- `DataProcess.__init__(data_queue)`：保存数据队列并初始化缓存、状态和信号。
- `DataProcess._get_sensor_range_index()`：读取当前探头量程索引。
- `DataProcess._get_sensor_range_text()`：读取当前探头量程显示文本。
- `DataProcess._get_mag_conversion_factor()`：按探头量程返回磁场换算系数。
- `DataProcess._get_default_offset()`：读取当前默认偏置值。
- `DataProcess.set_sample_info(sample_info)`：设置待写入数据文件头的样品信息。
- `DataProcess.stop_measure_processing()`：停止当前测量数据处理。
- `DataProcess.update_sample_info(key, value)`：更新单个样品信息字段。
- `DataProcess.get_sample_info()`：返回当前样品信息副本。
- `DataProcess._emit_measurement_results(measure_list)`：持久化测量数据并发出处理完成信号。
- `DataProcess._lowpass_filter(data_list)`：对磁场数据做低通滤波。
- `DataProcess.clear_data_queue()`：清空输入队列。
- `DataProcess.clear_self_detect_buffer()`：清空自检文本缓冲区。
- `DataProcess._append_self_detect_text(text)`：追加自检文本片段。
- `DataProcess._text_preview(text, limit)`：生成日志用的短文本预览。
- `DataProcess.get_self_detect_buffer_preview()`：获取自检缓冲区预览。
- `DataProcess.check_self_detect()`：检查设备自检完成消息。
- `DataProcess.process_position_data()`：解析位置数据并发出位置更新信号。
- `DataProcess.process_measure_data()`：收集测量字节流并触发测量处理。
- `DataProcess.process_offset_data()`：处理偏置校准采集数据。
- `DataProcess._candidate_range(lower, upper, max_count)`：生成闭合点候选搜索范围。
- `DataProcess._slope_sign(value, threshold)`：按阈值判断斜率方向。
- `DataProcess._classify_local_shape(data, index, span, value_scale)`：识别局部波形形态。
- `DataProcess._score_closure_candidate(...)`：给候选闭合点打分。
- `DataProcess._estimate_cycle_count(segment)`：估算一圈数据中的磁周期数。
- `DataProcess._estimate_period_points(segment, fallback_points)`：估算单个磁周期点数。
- `DataProcess._find_best_rotation_closure(...)`：在粗略尾点附近查找最佳闭合点。
- `DataProcess._process_measure_algorithm(measure_list)`：测量数据主处理算法。
- `DataProcess.save_raw_measure_data(measure_list)`：保存原始测量数据 CSV。
- `DataProcess.save_plot_measure_data(angle_data, mag_data, analysis_results)`：保存处理后的角度、磁场和分析结果 CSV。
- `DataProcess.stop()`：停止数据处理器。

### `src/core/logger.py`

文件作用：统一配置应用日志，支持控制台与文件输出。

函数说明：

- `setup_logging(level, log_file, console)`：配置根日志等级、文件 handler 和控制台 handler。
- `get_logger(name)`：获取指定名称的 logger。
- `init_logging(log_dir)`：初始化默认日志目录和日志文件。
- `get_default_logger()`：获取默认 logger。

### `src/core/offset_calibration_config.py`

文件作用：定义偏置校准相关的共享时长配置，供 UI 进度和数据采集逻辑统一使用。

函数/类说明：无函数，主要提供常量。

### `src/core/path_utils.py`

文件作用：处理源码运行与 PyInstaller 打包运行时的路径差异。

函数说明：

- `get_runtime_root()`：返回源码运行根目录或冻结程序所在目录。
- `get_project_root()`：返回项目根目录或 exe 目录。
- `get_config_path(config_file)`：生成运行时配置文件路径。
- `get_data_dir(*parts)`：生成运行时数据目录路径。

### `src/core/serial_command.py`

文件作用：封装设备命令、位置查询、运动方案执行、自检贴靠、偏置校准等串口操作，是 UI 操作与底层串口写入之间的命令协调层。

类/函数说明：

- `WorkState`：工作状态枚举，用于区分空闲、测量、偏置、移动等状态。
- `SerialCommand`：串口命令协调器，负责发送命令并管理异步流程。
- `SerialCommand.__init__(thread_manager)`：接入线程管理器、队列和信号，初始化状态。
- `SerialCommand.command_lock(allow_position_query)`：命令锁上下文，防止互斥操作并发。
- `SerialCommand._setup_async_timers()`：创建异步命令相关定时器。
- `SerialCommand._begin_async_command_lock(allow_position_query)`：进入异步命令锁。
- `SerialCommand._end_async_command_lock()`：释放异步命令锁。
- `SerialCommand._can_start_async_operation(operation_name)`：检查是否允许启动新的异步操作。
- `SerialCommand._drop_pending_position_query_writes(reason)`：丢弃排队的位置查询写入。
- `SerialCommand._clear_position_wait()`：清除位置等待状态。
- `SerialCommand._get_axis_pulses_per_mm(axis)`：获取指定轴每毫米脉冲数。
- `SerialCommand._get_position_tolerance_pulse(axis)`：获取指定轴到位容差。
- `SerialCommand._start_position_wait(axis, target, callback, timeout_ms)`：开始等待指定轴到达目标位置。
- `SerialCommand._poll_position_wait()`：轮询位置等待状态。
- `SerialCommand._handle_position_wait_update(position_data)`：处理位置反馈并判断是否到位。
- `SerialCommand._on_position_wait_timeout()`：处理位置等待超时。
- `SerialCommand._resolve_step_target(step, target_x, target_z)`：把方案步骤解析成实际目标轴和坐标。
- `SerialCommand._finish_movement_sequence(success)`：结束运动方案执行。
- `SerialCommand._on_movement_step_finished(success)`：单个运动步骤完成后的回调。
- `SerialCommand._run_next_movement_step()`：执行运动方案中的下一步。
- `SerialCommand._start_movement_sequence(name, steps, target_x, target_z)`：启动串行运动方案。
- `SerialCommand._stop_self_detect()`：停止自检贴靠流程。
- `SerialCommand._poll_self_detect()`：轮询自检结果。
- `SerialCommand._on_self_detect_timeout()`：处理自检超时。
- `SerialCommand._start_self_detect(command, retract_axis, detect_axis)`：启动贴靠自检流程。
- `SerialCommand.send_data(data, source)`：向串口写队列发送命令。
- `SerialCommand.enable_position_query_timer(interval)`：开启位置查询定时器。
- `SerialCommand.disable_position_query_timer()`：关闭位置查询定时器。
- `SerialCommand._position_query_from_timer()`：定时发送位置查询。
- `SerialCommand.claw_rotate()`：启动夹爪/旋转测量相关动作。
- `SerialCommand.claw_stop()`：停止旋转动作。
- `SerialCommand.vertical_move(distance)`：执行垂直方向移动。
- `SerialCommand.auto_press()`：执行 Z 轴下压贴靠。
- `SerialCommand.auto_press_left()`：执行 X 轴左贴靠。
- `SerialCommand._on_self_detect_finished(axis)`：自检完成后处理回弹或后续动作。
- `SerialCommand._on_position_data_processed(position_data)`：接收位置数据并驱动等待流程。
- `SerialCommand._execute_retract(position_data)`：执行贴靠后的回弹动作。
- `SerialCommand._execute_move_task(position_data)`：执行手动相对移动任务。
- `SerialCommand.set_move_task(axis, direction, distance_mm)`：设置手动移动任务。
- `SerialCommand.position_query(source)`：发送位置查询命令。
- `SerialCommand.counter_measurer()`：启动计数/测量命令。
- `SerialCommand._emit_offset_process_signal()`：通知数据处理开始偏置处理。
- `SerialCommand._stop_offset_collection_if_needed()`：必要时停止偏置采集。
- `SerialCommand.slider_reset()`：滑台复位/零位校准。
- `SerialCommand.move_x(position)`：移动 X 轴到指定位置。
- `SerialCommand.move_z(position)`：移动 Z 轴到指定位置。
- `SerialCommand.execute_movement_scheme(steps, target_x, target_z)`：按方案串行执行移动步骤。
- `SerialCommand.suspend_position()`：移动到挂起位置。
- `SerialCommand.test_position()`：移动到测试位置。
- `SerialCommand.offset_calibration()`：启动偏置校准流程。
- `SerialCommand._on_offset_calibration_finished(success)`：偏置校准完成回调。

### `src/core/serial_manager.py`

文件作用：管理 `QSerialPort`，处理串口连接、断开、写队列、读缓冲、连接状态检测和异常断开。

类/函数说明：

- `SerialManager`：串口管理器。
- `SerialManager.__init__(read_queue, write_queue, thread_manager)`：初始化串口对象、队列和定时器。
- `SerialManager._normalize_serial_value(value, default)`：规范化串口参数值。
- `SerialManager._coerce_payload(data)`：把待写入数据转换为 bytes。
- `SerialManager._item_payload(item)`：从队列项中提取 payload。
- `SerialManager._is_mostly_printable(data)`：判断数据是否适合按文本打印。
- `SerialManager._payload_preview(data, limit)`：生成串口数据日志预览。
- `SerialManager._queue_ui_rx_payload(payload)`：缓存 UI 显示用接收数据。
- `SerialManager._flush_ui_rx_buffer()`：刷新 UI 接收缓冲。
- `SerialManager._log_rx_payload(payload)`：记录接收数据日志。
- `SerialManager.drop_pending_writes(command_prefix, reason)`：丢弃指定前缀的待写命令。
- `SerialManager.connect_serial(port)`：连接指定串口。
- `SerialManager.disconnect_serial()`：断开当前串口。
- `SerialManager._process_write()`：处理串口写队列。
- `SerialManager._on_bytes_written(byte_count)`：串口写入完成回调。
- `SerialManager._on_error_occurred(error)`：串口错误回调。
- `SerialManager._on_ready_read()`：串口接收数据回调。
- `SerialManager._check_connection_status()`：定时检查连接状态。
- `SerialManager._handle_connection_lost()`：处理连接丢失。
- `SerialManager.start()`：启动管理器定时器。
- `SerialManager.stop()`：停止管理器并关闭串口。
- `SerialManager.get_connection_status()`：返回当前连接状态。

### `src/core/thread_manager.py`

文件作用：统一创建和管理串口线程、数据处理线程、队列和跨线程信号连接。

类/函数说明：

- `ThreadManager`：线程管理器。
- `ThreadManager.__init__()`：创建队列、信号对象和线程对象占位。
- `ThreadManager.initialize_threads()`：初始化串口管理器、命令管理器和数据处理器。
- `ThreadManager.start_threads()`：启动后台线程。
- `ThreadManager.stop_threads()`：停止后台线程。
- `ThreadManager.cleanup()`：释放线程和对象资源。

### `src/ui/theme.py`

文件作用：提供全局浅色工业风 Qt 样式表。

函数说明：

- `get_base_stylesheet()`：返回基础样式表。
- `get_card_stylesheet(bg_color)`：返回卡片容器样式。
- `get_panel_stylesheet()`：返回面板样式。
- `get_dark_palette()`：返回调色板对象。

### `src/ui/window_operations.py`

文件作用：为无边框窗口提供拖拽、最小化、最大化/还原和关闭操作。

类/函数说明：

- `WindowOperations`：窗口操作封装类。
- `WindowOperations.__init__(window)`：绑定目标窗口并初始化拖拽状态。
- `WindowOperations.show_window()`：显示窗口。
- `WindowOperations.eventFilter(obj, event)`：拦截窗口标题区域鼠标事件。
- `WindowOperations._handle_mouse_press(event)`：处理拖拽起始。
- `WindowOperations._handle_mouse_move(event)`：处理拖拽移动。
- `WindowOperations._handle_mouse_release(event)`：结束拖拽。
- `WindowOperations.minimize_window()`：最小化窗口。
- `WindowOperations.maximize_window()`：最大化或还原窗口。
- `WindowOperations.close_window()`：关闭窗口。
- `create_window_operations(window)`：创建 `WindowOperations` 的便捷函数。

### `src/windows/__init__.py`

文件作用：声明窗口模块包，说明当前采用单窗口架构。

函数/类说明：无运行逻辑。

### `src/windows/main_panel.py`

文件作用：主窗口壳，加载各功能面板，处理左侧导航、页面切换和底部状态栏。

类/函数说明：

- `MainPanel`：单窗口应用主界面。
- `MainPanel.__init__()`：加载 UI、创建窗口操作、初始化导航和子面板。
- `MainPanel._init_nav_buttons()`：初始化导航按钮映射和点击事件。
- `MainPanel._on_nav_clicked(panel_id)`：响应导航点击。
- `MainPanel._switch_panel(panel_id)`：切换当前显示面板。
- `MainPanel._load_panels()`：加载测量、配置、历史、比对面板。
- `MainPanel._setup_statusbar()`：初始化状态栏显示。
- `MainPanel._center_on_screen()`：窗口居中显示。
- `MainPanel.update_serial_status(connected, port)`：更新状态栏串口连接状态。
- `MainPanel.update_position_from_tuple(position_data)`：从 `(x, z)` 元组更新位置显示。
- `MainPanel.update_position(x, z)`：更新底部坐标文本。
- `MainPanel.get_panel(panel_id)`：按面板 ID 获取面板对象。
- `MainPanel.get_active_panel()`：获取当前活动面板。

### `src/windows/measure_panel.py`

文件作用：测量主界面，负责样品信息、测试类型/速度/量程显示、串口连接、手动运动、贴靠、偏置校准、测量启动/停止、实时绘图、分析结果展示和数据保存。

类/函数说明：

- `MeasurePanel`：测量面板。
- `MeasurePanel.__init__()`：加载 UI 并初始化样品、配置、绘图和串口控件。
- `MeasurePanel._init_sample_info_inputs()`：初始化样品信息下拉框。
- `MeasurePanel._init_tester_combo()`：初始化测试人员下拉框。
- `MeasurePanel._init_sensor_combo()`：初始化探头量程下拉框。
- `MeasurePanel._on_sensor_range_changed(index)`：处理探头量程变化。
- `MeasurePanel._on_config_sensor_range_changed(index)`：同步配置面板量程变化。
- `MeasurePanel._sync_sensor_range_from_ui()`：从 UI 同步当前量程到配置。
- `MeasurePanel._load_tester_history()`：读取测试人员历史。
- `MeasurePanel._merge_tester_history(latest, history)`：合并测试人员历史。
- `MeasurePanel._set_combo_items(combo, items, current_text)`：设置下拉框内容并保持当前值。
- `MeasurePanel._combo_text(combo_name, fallback_line_edit_name)`：获取下拉框或输入框文本。
- `MeasurePanel._save_tester_history(tester)`：保存测试人员历史。
- `MeasurePanel._init_config_display()`：初始化测试配置显示。
- `MeasurePanel._update_scheme_display(test_type)`：刷新移动方案显示。
- `MeasurePanel._on_test_type_changed(index)`：处理测试类型变化。
- `MeasurePanel._on_test_speed_changed(index)`：处理测试速度变化。
- `MeasurePanel._on_config_test_type_changed(index)`：同步配置管理器测试类型变化。
- `MeasurePanel._on_config_test_speed_changed(index)`：同步配置管理器测试速度变化。
- `MeasurePanel._on_config_scheme_changed(test_type)`：同步移动方案变化。
- `MeasurePanel._init_plot_display()`：初始化绘图区域。
- `MeasurePanel._init_serial_controls()`：初始化嵌入式串口连接控件。
- `MeasurePanel._set_combo_text(combo_name, value)`：设置指定下拉框文本。
- `MeasurePanel._get_serial_port_from_ui()`：读取界面选中的串口号。
- `MeasurePanel._apply_serial_port_to_ui(com_port)`：把配置串口号应用到界面。
- `MeasurePanel._set_serial_status(text, color)`：更新串口状态文字和颜色。
- `MeasurePanel._set_serial_button_state(connected, connecting)`：根据连接状态切换单个连接按钮文本和样式。
- `MeasurePanel._save_serial_port(com_port)`：保存串口号配置。
- `MeasurePanel._refresh_ports()`：扫描并刷新可用串口列表。
- `MeasurePanel._start_connect(com_port, status_text)`：发起串口连接。
- `MeasurePanel._set_port_refresh_enabled(enabled)`：启停串口列表自动刷新。
- `MeasurePanel.showEvent(event)`：面板显示时刷新串口/状态。
- `MeasurePanel.hideEvent(event)`：面板隐藏时暂停刷新。
- `MeasurePanel._on_connect_clicked()`：单按钮连接/断开串口。
- `MeasurePanel._on_connection_timeout()`：处理串口连接超时。
- `MeasurePanel._on_serial_status_changed(connected)`：同步串口连接状态到 UI。
- `MeasurePanel.auto_connect_from_config()`：按配置自动连接串口。
- `MeasurePanel._connect_buttons()`：绑定界面按钮事件。
- `MeasurePanel._has_current_waveform_analysis()`：检查当前是否有可展示的分析结果。
- `MeasurePanel._show_analysis_detail(detail_type)`：打开分析详情弹窗。
- `MeasurePanel._remove_analysis_detail_dialog(dialog)`：从缓存中移除已关闭详情弹窗。
- `MeasurePanel._clear_status_message()`：清空状态提示。
- `MeasurePanel._start_rotation_button_clicked()`：开始测量流程。
- `MeasurePanel._on_test_cancel()`：取消正在进行的测试。
- `MeasurePanel._stop_rotation_button_clicked()`：停止旋转。
- `MeasurePanel._zeroing_button_clicked()`：执行零位校准/滑台复位。
- `MeasurePanel._offset_button_clicked()`：启动偏置校准。
- `MeasurePanel._on_offset_calibration_finished(success)`：处理偏置校准结果。
- `MeasurePanel._close_offset_dialog()`：关闭偏置校准弹窗。
- `MeasurePanel._test_position_button_clicked()`：移动到测试位置。
- `MeasurePanel._suspend_position_button_clicked()`：移动到挂起位置。
- `MeasurePanel._press_z_button_clicked()`：执行 Z 轴下压贴靠。
- `MeasurePanel._left_x_button_clicked()`：执行 X 轴左贴靠。
- `MeasurePanel._up_button_clicked()` / `_down_button_clicked()` / `_left_button_clicked()` / `_right_button_clicked()`：手动方向移动。
- `MeasurePanel._get_distance_value()`：读取手动移动距离。
- `MeasurePanel._reset_sample_inputs()`：重置样品信息输入。
- `MeasurePanel._update_display_defaults()`：恢复分析结果默认显示。
- `MeasurePanel._reset_test_interface()`：重置测量界面状态。
- `MeasurePanel._disable_function_buttons()`：测量中禁用功能按钮。
- `MeasurePanel._enable_function_buttons()`：恢复功能按钮可用。
- `MeasurePanel._set_measurement_locked_buttons_enabled(enabled)`：统一设置测量锁定相关按钮状态。
- `MeasurePanel._end_test()`：结束测试并恢复 UI。
- `MeasurePanel._apply_status_message(message, is_error)`：设置状态提示文本。
- `MeasurePanel._update_status(message, is_error, auto_recover)`：更新状态提示并可自动恢复。
- `MeasurePanel.show_history_file_status(file_path)`：显示已加载历史文件状态。
- `MeasurePanel._collect_sample_info_from_ui()`：收集样品信息。
- `MeasurePanel.update_plot_data(angle_data, mag_data, color)`：更新绘图数据。
- `MeasurePanel.clear_plot()`：清除当前图形。
- `MeasurePanel.save_plot_data()`：保存当前绘图数据。
- `MeasurePanel._save_data_button_clicked()`：保存按钮回调。
- `MeasurePanel.set_thread_manager(tm)`：注入线程管理器并连接信号。
- `MeasurePanel._on_position_data_updated(position_data)`：接收位置更新。
- `MeasurePanel._on_measure_progress(current, total)`：更新测量进度。
- `MeasurePanel._on_measure_data_processed_legacy(angle_data, mag_data)`：兼容旧版测量完成信号。
- `MeasurePanel._on_measure_data_processed(angle_data, mag_data, analysis_results)`：处理测量完成数据和分析结果。
- `MeasurePanel._close_test_progress_dialog()`：关闭测试进度弹窗。
- `MeasurePanel._update_display_with_results(results)`：把分析结果显示到界面。

### `src/windows/test_config_panel.py`

文件作用：测试配置页面，负责串口连接、测试类型/速度/量程、移动方案编辑、测试/挂起位置保存、快捷操作、贴靠参数和偏移量配置。

类/函数说明：

- `TestConfigPanel`：测试配置面板。
- `TestConfigPanel.__init__()`：加载 UI 并初始化配置、串口和按钮事件。
- `TestConfigPanel.set_thread_manager(tm)`：注入线程管理器并连接信号。
- `TestConfigPanel._init_test_mode_and_scheme()`：初始化测试模式和移动方案。
- `TestConfigPanel._update_scheme_display(test_type)`：刷新指定测试类型的方案显示。
- `TestConfigPanel._on_edit_test_scheme()`：打开测试位置方案编辑器。
- `TestConfigPanel._on_suspend_edit_scheme()`：打开挂起位置方案编辑器。
- `TestConfigPanel._on_test_type_changed(index)`：保存测试类型变化。
- `TestConfigPanel._on_test_speed_changed(index)`：保存测试速度变化。
- `TestConfigPanel._on_sensor_range_changed(index)`：保存探头量程变化。
- `TestConfigPanel._on_config_sensor_range_changed(index)`：同步外部量程变化。
- `TestConfigPanel._sync_sensor_range_from_ui()`：从界面同步量程到配置。
- `TestConfigPanel._on_config_test_type_changed(index)`：同步外部测试类型变化。
- `TestConfigPanel._on_config_test_speed_changed(index)`：同步外部测试速度变化。
- `TestConfigPanel._on_config_scheme_changed(test_type)`：同步外部移动方案变化。
- `TestConfigPanel._load_saved_positions()`：读取保存的测试/挂起位置。
- `TestConfigPanel._init_serial_controls()`：初始化串口连接控件。
- `TestConfigPanel._set_combo_text(combo_name, value)`：设置下拉框文本。
- `TestConfigPanel._get_serial_port_from_ui()`：读取界面串口号。
- `TestConfigPanel._apply_serial_port_to_ui(com_port)`：应用配置串口号到界面。
- `TestConfigPanel._set_serial_status(text, color)`：更新串口状态文本。
- `TestConfigPanel._set_serial_button_state(connected, connecting)`：切换单个连接按钮文本和样式。
- `TestConfigPanel._save_serial_port(com_port)`：保存串口号。
- `TestConfigPanel._refresh_ports()`：刷新可用串口列表。
- `TestConfigPanel._start_connect(com_port, status_text)`：发起连接。
- `TestConfigPanel._set_port_refresh_enabled(enabled)`：启停串口列表刷新。
- `TestConfigPanel.showEvent(event)`：显示时同步串口状态。
- `TestConfigPanel.hideEvent(event)`：隐藏时暂停刷新。
- `TestConfigPanel._on_connect_clicked()`：单按钮连接/断开串口。
- `TestConfigPanel._on_connection_timeout()`：处理连接超时。
- `TestConfigPanel._on_serial_status_changed(connected)`：同步串口状态到 UI。
- `TestConfigPanel._connect_buttons()`：绑定按钮事件。
- `TestConfigPanel._init_quick_action_settings()`：初始化快捷操作参数控件。
- `TestConfigPanel._bind_double_spin(object_name, value, handler)`：绑定浮点输入控件与配置保存回调。
- `TestConfigPanel._on_retract_distance_changed(value)`：保存贴靠回弹距离。
- `TestConfigPanel._on_x_offset_changed(value)`：保存 X 偏移量。
- `TestConfigPanel._on_z_offset_changed(value)`：保存 Z 偏移量。
- `TestConfigPanel._on_position_data_updated(position_data)`：接收位置数据并更新显示。
- `TestConfigPanel._on_zeroing()`：执行零位校准/滑台复位。
- `TestConfigPanel._on_offset()`：启动偏置校准。
- `TestConfigPanel._on_offset_calibration_finished(success)`：处理偏置校准结果。
- `TestConfigPanel._close_offset_dialog()`：关闭偏置校准弹窗。
- `TestConfigPanel._log_adhesion_button(action)`：记录贴靠按钮上下文日志。
- `TestConfigPanel._on_press_z()`：执行 Z 轴下压贴靠。
- `TestConfigPanel._on_left_x()`：执行 X 轴左贴靠。
- `TestConfigPanel._on_test_pos()`：移动到测试位置。
- `TestConfigPanel._on_suspend()`：移动到挂起位置。
- `TestConfigPanel._on_test_pos_save()`：保存当前测试位置。
- `TestConfigPanel._on_suspend_save()`：保存当前挂起位置。
- `TestConfigPanel.update_position(x, z)`：更新位置显示。

### `src/windows/plot_window.py`

文件作用：封装 pyqtgraph 绘图控件，提供坐标轴、曲线、网格、范围、双击复位和中心文本显示能力。

类/函数说明：

- `PlotWindow`：绘图窗口/绘图控件封装。
- `PlotWindow.__init__(parent)`：初始化绘图状态并创建控件。
- `PlotWindow._init_plot_widget()`：创建 `PlotWidget`、曲线、坐标轴和交互。
- `PlotWindow._setup_x_axis()`：设置固定 X 轴刻度。
- `PlotWindow._setup_plot_layout()`：把绘图控件放入布局。
- `PlotWindow._on_plot_double_click(event)`：双击恢复默认视图。
- `PlotWindow._on_view_range_changed(view_box, view_range)`：视图范围变化后延迟刷新刻度。
- `PlotWindow._update_adaptive_ticks()`：刷新固定 X 轴和自适应 Y 轴刻度。
- `PlotWindow._calculate_tick_interval(span)`：按范围计算合适刻度间隔。
- `PlotWindow._update_y_axis_ticks(y_range, tick_interval)`：更新 Y 轴刻度文本。
- `PlotWindow.reset_plot_view()`：恢复默认坐标范围。
- `PlotWindow.update_plot(angle_data, mag_data, color, auto_y_range)`：更新整条曲线。
- `PlotWindow.add_plot_data(angle_data, mag_data, color)`：追加绘图数据。
- `PlotWindow.clear_plot()`：清除曲线和缓存。
- `PlotWindow.set_x_range(min_val, max_val)`：设置 X 轴范围。
- `PlotWindow.set_y_range(min_val, max_val)`：设置 Y 轴范围。
- `PlotWindow.auto_range()`：自动调整显示范围。
- `PlotWindow.get_plot_widget()`：返回底层绘图控件。
- `PlotWindow.set_background_color(color)`：设置背景色。
- `PlotWindow.set_grid_visibility(x_visible, y_visible, alpha)`：设置网格显示。
- `PlotWindow.set_curve_style(color, width, style)`：设置曲线样式。
- `PlotWindow.init_plot_display(parent_widget)`：把绘图控件挂载到父控件。
- `PlotWindow.show_text_in_center(text, color, size)`：显示中心提示文本。
- `PlotWindow.remove_text_item(text_item)`：移除中心提示文本。
- `PlotDataProcessor`：绘图数据缓存处理类。
- `PlotDataProcessor.__init__()`：初始化角度和磁场数据缓存。
- `PlotDataProcessor.process_magnetic_data(raw_data_queue)`：从原始队列转换绘图数据。
- `PlotDataProcessor.clear_data()`：清除缓存。

### `src/windows/wave_analysis.py`

文件作用：波形分析算法模块，负责过零点、极值、N/S 半波间隔、周期误差、面积、THD 和极对数等指标计算。

类/函数说明：

- `WaveAnalysis`：波形分析器。
- `WaveAnalysis.__init__()`：初始化分析器。
- `WaveAnalysis._get_zero_angle_merge_tolerance(x)`：计算过零点角度合并容差。
- `WaveAnalysis._get_zero_value_tolerance(y)`：计算磁场零值容差。
- `WaveAnalysis._normalize_zero_angles(zero_angles, x)`：去除重复过零点并合并闭环首尾零点。
- `WaveAnalysis._build_circular_polar_intervals(zero_angles, x, y)`：按闭环过零点计算 N/S 半波和单对极周期。
- `WaveAnalysis.analyze_waveform(angle_data, mag_data, enable_concentricity_calibration)`：对外分析入口。
- `WaveAnalysis._wave_analysis(x, y, enable_concentricity_calibration)`：波形分析核心算法。

### `src/windows/analysis_detail_dialog.py`

文件作用：展示波形分析的详细数据，包括过零点、极值点、周期误差统计和对应图表。

类/函数说明：

- `AnalysisDetailDialog`：波形分析详情窗口。
- `AnalysisDetailDialog.__init__(detail_type, angle_data, mag_data, analysis_results, parent)`：保存分析上下文并初始化 UI。
- `AnalysisDetailDialog._init_ui()`：创建表格和绘图布局。
- `AnalysisDetailDialog._populate()`：按详情类型填充内容。
- `AnalysisDetailDialog._plot_waveform()`：绘制基础波形。
- `AnalysisDetailDialog._populate_zero_crossing()`：填充过零点详情。
- `AnalysisDetailDialog._plot_zero_crossing_waveform(details)`：绘制过零点波形标注。
- `AnalysisDetailDialog._plot_zero_crossing_intervals(details, mean_interval)`：绘制过零间隔图。
- `AnalysisDetailDialog._populate_extreme_point()`：填充极值点详情。
- `AnalysisDetailDialog._populate_period_error()`：填充周期误差详情。
- `AnalysisDetailDialog._set_stats_table(rows)`：设置统计表格内容。
- `AnalysisDetailDialog._set_detail_table(headers, rows)`：设置明细表格内容。
- `AnalysisDetailDialog._mean(values)`：计算均值。
- `AnalysisDetailDialog._max(values)`：计算最大值。
- `AnalysisDetailDialog._min(values)`：计算最小值。
- `AnalysisDetailDialog._std(values)`：计算标准差。
- `AnalysisDetailDialog._peak_to_peak(values)`：计算峰峰值。
- `AnalysisDetailDialog._is_number(value)`：判断是否为数值。
- `AnalysisDetailDialog._format_value(value)`：格式化数值。
- `AnalysisDetailDialog._format_value_with_unit(value, unit)`：格式化带单位的数值。

### `src/windows/history_panel.py`

文件作用：历史数据列表页面，后台扫描 `plot_data` CSV，支持筛选、刷新、双击加载到测量界面。

类/函数说明：

- `_is_plot_data_header(row)`：判断 CSV 行是否为波形数据表头。
- `_apply_sample_info_row(sample_info, row)`：把 CSV 元数据行写入样品信息字典。
- `_read_plot_csv_header(file_path)`：只读取 CSV 元数据，用于历史表格。
- `_read_plot_csv(file_path)`：读取 CSV 元数据和波形数据。
- `LoadHistoryThread`：后台加载历史数据线程。
- `LoadHistoryThread.__init__(plot_data_dir, parent)`：设置历史数据目录。
- `LoadHistoryThread.run()`：扫描历史 CSV 并发出记录列表。
- `HistoryPanel`：历史数据面板。
- `HistoryPanel.__init__()`：加载 UI 并初始化表格和事件。
- `HistoryPanel.showEvent(event)`：面板显示时自动刷新。
- `HistoryPanel._connect_buttons()`：绑定搜索、刷新和表格事件。
- `HistoryPanel._setup_table()`：初始化表头和列宽。
- `HistoryPanel._get_plot_data_dir()`：获取历史波形数据目录。
- `HistoryPanel._start_load_history()`：启动后台加载线程。
- `HistoryPanel._on_load_history_finished(records)`：加载完成后填充表格。
- `HistoryPanel._on_search_changed()`：执行搜索筛选。
- `HistoryPanel._on_refresh()`：刷新历史列表。
- `HistoryPanel._on_table_double_clicked(item)`：双击历史记录。
- `HistoryPanel._load_history_to_measure(file_path)`：读取历史文件并准备回填。
- `HistoryPanel._switch_to_measure_and_load(angle_data, mag_data, sample_info, file_path)`：切回测量面板并加载历史波形。

### `src/windows/compare_panel.py`

文件作用：数据比对页面，选择两个历史 CSV 文件，绘制两条曲线并显示分析结果对比。

类/函数说明：

- `ComparePanel`：数据比对面板。
- `ComparePanel.__init__()`：加载 UI 并初始化按钮和绘图。
- `ComparePanel._connect_buttons()`：绑定浏览和比对按钮。
- `ComparePanel._init_plot_widget()`：初始化比对图表。
- `ComparePanel.eventFilter(obj, event)`：捕获绘图区域双击。
- `ComparePanel._on_plot_double_click()`：双击时在独立窗口查看曲线。
- `ComparePanel._on_browse(file_num)`：选择第一个或第二个 CSV 文件。
- `ComparePanel._get_plot_data_dir()`：获取默认历史数据目录。
- `ComparePanel._read_csv_file(file_path)`：读取 CSV 角度、磁场和元数据。
- `ComparePanel._analyze_data(angle_data, mag_data)`：调用波形分析算法。
- `ComparePanel._format_results(results)`：格式化分析结果文本。
- `ComparePanel._on_compare()`：执行两文件比对。
- `ComparePanel._update_plot()`：更新比对曲线。

### `src/windows/measure_type_dialog.py`

文件作用：测量类型选择弹窗，用于选择旋转测量或垂直测量并读取垂直距离。

类/函数说明：

- `MeasureTypeDialog`：测量类型选择弹窗。
- `MeasureTypeDialog.__init__(parent)`：加载 UI 并初始化拖拽状态。
- `MeasureTypeDialog.mousePressEvent(event)`：处理窗口拖拽按下。
- `MeasureTypeDialog.mouseMoveEvent(event)`：处理窗口拖拽移动。
- `MeasureTypeDialog.mouseReleaseEvent(event)`：结束窗口拖拽。
- `MeasureTypeDialog._on_rotation_clicked()`：选择旋转测量。
- `MeasureTypeDialog._on_vertical_clicked()`：选择垂直测量。
- `MeasureTypeDialog.get_measure_type()`：返回选择的测量类型。
- `MeasureTypeDialog.get_vertical_distance()`：返回垂直测量距离。

### `src/windows/offset_calibration_dialog.py`

文件作用：偏置校准进度和结果弹窗。

类/函数说明：

- `OffsetCalibrationDialog`：偏置校准对话框。
- `OffsetCalibrationDialog.__init__(parent)`：加载 UI 并初始化计时器。
- `OffsetCalibrationDialog.mousePressEvent(event)`：处理窗口拖拽按下。
- `OffsetCalibrationDialog.mouseMoveEvent(event)`：处理窗口拖拽移动。
- `OffsetCalibrationDialog.mouseReleaseEvent(event)`：结束窗口拖拽。
- `OffsetCalibrationDialog.start_progress(duration)`：按预计时长启动进度更新。
- `OffsetCalibrationDialog._update_progress_by_time()`：根据经过时间更新进度条。
- `OffsetCalibrationDialog.stop_progress()`：停止进度更新。
- `OffsetCalibrationDialog.set_progress(value, text)`：设置进度值和状态文本。
- `OffsetCalibrationDialog.show_result(success, offset_value)`：显示校准成功或失败结果。

### `src/windows/test_progress_dialog.py`

文件作用：测量过程进度和结果弹窗。

类/函数说明：

- `TestProgressDialog`：测试进度对话框。
- `TestProgressDialog.__init__(parent)`：加载 UI 并初始化拖拽状态。
- `TestProgressDialog.mousePressEvent(event)`：处理窗口拖拽按下。
- `TestProgressDialog.mouseMoveEvent(event)`：处理窗口拖拽移动。
- `TestProgressDialog.mouseReleaseEvent(event)`：结束窗口拖拽。
- `TestProgressDialog.set_progress(value, text)`：设置进度条和状态文本。
- `TestProgressDialog.show_result(success, message)`：显示测试成功或失败结果。
- `TestProgressDialog.set_title(text)`：设置标题。
- `TestProgressDialog.set_status(text)`：设置状态文本。

### `src/windows/scheme_edit_dialog.py`

文件作用：运动方案编辑弹窗，用于新增、删除、排序和保存测试/挂起移动步骤。

类/函数说明：

- `SchemeEditDialog`：方案编辑对话框。
- `SchemeEditDialog.__init__(scheme_data, parent)`：接收原始方案并初始化 UI。
- `SchemeEditDialog.init_ui()`：构建列表和操作按钮。
- `SchemeEditDialog.load_steps()`：把方案步骤加载到列表。
- `SchemeEditDialog.add_step()`：新增步骤。
- `SchemeEditDialog.delete_step()`：删除当前选中步骤。
- `SchemeEditDialog.move_step_up()`：上移当前步骤。
- `SchemeEditDialog.move_step_down()`：下移当前步骤。
- `SchemeEditDialog.get_result()`：返回编辑后的方案。

## 五、维护建议

1. 新增 UI 按钮槽函数时，建议在绑定处附近保留简短说明，避免后续静态清理误判为无调用。
2. 对复杂算法函数建议补充 docstring，尤其是 `DataProcess` 的闭合点搜索和 `WaveAnalysis` 的闭环过零点逻辑。
3. 属性 getter/setter 不必强行逐个写长注释，但配置字段含义应优先在 `README.md` 和本文档中保持同步。
4. 串口命令和运动流程属于高风险区域，修改 `serial_command.py` 时建议同步更新本文档对应函数说明。
5. 运行前后可使用以下命令做快速语法检查：

```powershell
python -m compileall -q src
```
