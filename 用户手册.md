# MARS 旋转体表磁测量分析系统用户手册

更新日期：2026-06-30

## 目录

1. [系统简介](#1-系统简介)
2. [快速开始](#2-快速开始)
3. [界面介绍](#3-界面介绍)
4. [测量操作](#4-测量操作)
5. [历史数据与数据比对](#5-历史数据与数据比对)
6. [配置、数据与日志](#6-配置数据与日志)
7. [打包版本说明](#7-打包版本说明)
8. [常见问题](#8-常见问题)
9. [附录：串口命令参考](#附录串口命令参考)

---

## 1. 系统简介

MARS（Magnetic Analysis of Rotating Surfaces）是一款用于旋转体表磁测量的桌面软件。系统通过串口控制硬件执行贴靠、移动、校准和旋转采集，并对采集到的磁场波形进行分析。

主要能力：

- 串口通信和自动连接
- X/Z 轴位置查询、手动移动、零位校准
- 下压贴靠、左贴靠和贴靠回弹
- 测试位置 / 挂起位置保存与移动
- 测试 / 挂起移动流程编辑
- 偏置校准（8 秒采集，取中间 3 秒稳定段）
- 80mT / 160mT 探头量程切换
- 高速测量 / 高分辨率测量切换
- 旋转测量与垂直测量
- 波形显示、结果分析、历史数据加载、双文件比对
- 一致性测试工具（批量分析多次测量稳定性）

系统要求：

- Windows 10/11
- 串口硬件设备
- 源码运行需要 Python 3.8+ 和 `requirements.txt` 中的依赖

---

## 2. 快速开始

### 2.1 源码运行

```powershell
pip install -r requirements.txt
python src/main.py
```

### 2.2 打包版运行

双击运行：

```powershell
dist/MARS/MARS.exe
```

### 2.3 首次使用流程

1. 连接硬件设备。
2. 打开“串口设置”，选择串口号、波特率、数据位、停止位和校验位。
3. 点击“连接”，确认底部状态栏显示已连接。
4. 打开“测试位置”，执行零位校准或贴靠动作。
5. 保存测试位置和挂起位置。
6. 按需要编辑当前测试类型的测试流程和挂起流程。
7. 执行偏置校准。
8. 回到“测量界面”，填写样品信息并开始测量。
9. 测量完成后查看结果，必要时保存数据。

---

## 3. 界面介绍

### 3.1 主窗口

左侧导航包含：

| 导航项 | 用途 |
| --- | --- |
| 测量界面 | 采集波形、查看分析结果、保存数据 |
| 串口设置 | 配置串口参数、连接/断开设备、查看串口收发 |
| 测试位置 | 位置查询、贴靠、测试/挂起位置管理、移动流程设置 |
| 历史数据 | 浏览、筛选并双击加载历史数据 |
| 数据比对 | 加载两份历史文件并对比波形和分析结果 |

窗口底部会显示串口连接状态和当前位置。

### 3.2 串口设置

可配置项：

- 串口号：自动扫描系统可用串口
- 波特率：默认 `921600`
- 数据位：默认 `8`
- 停止位：默认 `1`
- 校验位：默认 `无`

连接成功后，程序会保存串口配置，下次启动会尝试自动连接。

### 3.3 测试位置

测试位置界面用于机械运动和测试流程配置。

常用操作：

- “自动下压”：执行 Z 轴贴靠。
- “自动左压 / 自动右压”：执行 X 轴左右贴靠。
- “回弹距离”：设置贴靠完成后的回弹距离。
- “保存测试位置”：将当前 X/Z 坐标保存为测试位置。
- “保存挂起位置”：将当前 X/Z 坐标保存为挂起位置。
- “移动到测试位置 / 移动到挂起位置”：按当前流程移动到对应位置。
- “零位校准”：滑台复位回原点。
- “偏置校准”：采集无被测磁场时的偏置值。

测试类型支持：

- 平面旋转测试
- 外侧面旋转测试
- 内侧面旋转测试
- 外侧面垂直测试

每种测试类型都有独立的测试流程和挂起流程。流程由动作步骤组成，例如：

- `移动到X目标`
- `移动到Z目标`
- `X正偏移`
- `X负偏移`
- `Z正偏移`
- `Z负偏移`

测试配置界面中编辑后的流程会同步显示到测量界面。

### 3.4 测量界面

测量界面包含四类区域：

| 区域 | 说明 |
| --- | --- |
| 测试配置 | 显示当前测试类型、测试位置步骤、挂起位置步骤 |
| 样品信息 | 填写样品名称、样品编号、测试员、气隙和备注 |
| 按键操作 | 开始/停止测量、校准、移动到测试/挂起位置、方向微调 |
| 数据分析结果 | 显示 N/S 极、间隔、面积、THD、极对数等分析结果 |

波形显示说明：

- 默认测量波形为红色。
- 从历史数据加载的波形为蓝色。
- 双击波形区域会恢复默认坐标范围，不会再弹出放大窗口。
- 默认坐标范围为 X 轴 `0-360`，Y 轴 `-70-70`。

### 3.5 分析结果字段

| 字段 | 说明 |
| --- | --- |
| N_max / N_min / N_mean / N_se | N 极最大值、最小值、平均值和误差 |
| S_max / S_min / S_mean / S_se | S 极最大值、最小值、平均值和误差 |
| NS_2 | N/S 极峰值平均关系 |
| N/S_interval_max | N/S 极间隔最大值 |
| N/S_interval_min | N/S 极间隔最小值 |
| N/S_interval_mean | N/S 极间隔平均值 |
| N/S_interval_std | N/S 极间隔误差 |
| N_area / S_area / NS_area | N 极面积、S 极面积和总面积 |
| SinglePolarMean | 单对极周期平均值 |
| SinglePolarError | 单极误差最大值 |
| PolarErrorSum | 极误差累计峰峰值 |
| THD_error | 总谐波失真率 |
| pole_num | 极对数 |

---

## 4. 测量操作

### 4.1 旋转测量

1. 在“测试位置”界面保存测试位置和挂起位置。
2. 在测试类型中选择实际测试类型。
3. 确认测试流程和挂起流程无误。
4. 在“测量界面”填写样品信息。
5. 按需勾选“启用同心度校准”。
6. 点击“开始测量”。
7. 等待进度对话框完成。
8. 查看波形和分析结果。
9. 点击“保存数据”。

### 4.2 停止测量

测量过程中点击“停止测量”会发送停止旋转命令，并处理已采集到的有效数据。停止后位置查询会恢复。

### 4.3 偏置校准

偏置校准用于消除环境磁场影响。

1. 确保探头附近没有被测磁场。
2. 点击“偏置校准”。
3. 系统发送定时采集命令，采集约 `8` 秒，取中间 `3` 秒稳定段计算偏置值。
4. 进度对话框完成后显示偏置值。
5. 偏置值会写入配置文件，后续测量会自动使用。

偏置校准期间，程序会停止位置查询，避免位置返回数据干扰偏置数据处理。

---

## 5. 历史数据与数据比对

### 5.1 历史数据

历史数据界面会在每次打开时自动刷新列表。列表支持按样品名称、测试员、极数和气隙筛选。

打开历史数据：

- 双击表格中的记录即可加载。
- 加载后会自动切换到测量界面。
- 测量界面会显示当前加载文件的文件名提示。
- 历史波形使用蓝色绘制。

### 5.2 数据比对

数据比对流程：

1. 选择文件 1 和文件 2。
2. 点击"比对"。
3. 上方显示红/蓝双波形叠加图，下方为指标对比表格，三列分别为指标名称、文件1结果、文件2结果。
4. 双击波形区域可在独立窗口中放大查看（支持 OpenGL 加速）。

---

## 6. 配置、数据与日志

### 6.1 配置文件

源码运行读取：

```text
src/configuration.txt
```

打包运行读取：

```text
dist/MARS/configuration.txt
```

常用配置项：

| 配置项 | 说明 |
| --- | --- |
| offset | 磁场偏置值 |
| COM | 串口号 |
| baudrate | 波特率 |
| bytesize | 数据位 |
| stopbits | 停止位 |
| parity | 校验位 |
| test_x / test_z | 测试位置坐标 |
| suspend_x / suspend_z | 挂起位置坐标 |
| test_type | 当前测试类型 |
| inner_x_offset | 内侧面 X 偏移量，单位 mm |
| inner_z_offset | 内侧面 Z 偏移量，单位 mm |
| sensor_range | 探头量程（0=80mT量程, 1=160mT量程） |
| test_speed | 测试速度（0=高速测量, 1=高分辨率测量） |
| retract_distance | 贴靠回弹距离，单位 mm |
| test_type_schemes | 各测试类型的测试/挂起流程配置 |

### 6.2 数据目录

源码运行：

| 数据 | 路径 |
| --- | --- |
| 原始数据 | `data/raw_data/` |
| 处理后波形数据 | `data/plot_data/` |

打包运行：

| 数据 | 路径 |
| --- | --- |
| 原始数据 | `dist/MARS/data/raw_data/` |
| 处理后波形数据 | `dist/MARS/data/plot_data/` |

### 6.3 日志目录

源码运行：

```text
src/logs/
```

打包运行：

```text
dist/MARS/logs/
```

日志文件按日期命名，便于排查串口收发、贴靠流程、偏置校准和测量处理问题。

---

## 7. 打包版本说明

打包命令：

```powershell
pyinstaller MARS.spec --clean --noconfirm
```

打包产物：

```text
dist/MARS/MARS.exe
```

打包时会导入：

- UI 文件目录
- 窗口代码目录
- 核心代码目录
- `configuration.txt`（本地文件存在时）
- `configuration.example.txt`
- `icon.png`

`src/icon.png` 是窗口图标源文件，`src/icon.ico` 是由 PNG 生成的可执行文件图标。如果打包时本地配置文件不存在，程序首次启动会自动创建 `dist/MARS/configuration.txt`。

---

## 8. 常见问题

### Q: 串口无法连接？

- 检查硬件设备是否连接。
- 确认串口号是否正确。
- 检查是否有其他软件占用串口。
- 尝试断开后重新连接。

### Q: 历史数据里看不到刚保存的数据？

- 进入历史数据界面时会自动刷新。
- 如果仍未出现，检查数据是否保存到当前运行方式对应的 `data/plot_data/`。

### Q: 偏置校准卡住或失败？

- 确认串口连接稳定。
- 校准期间不要执行其他位置操作。
- 查看日志中的 `Offset flow` 记录。

### Q: 贴靠按钮点击后无动作？

- 检查串口连接状态。
- 确认硬件回信是否正常。
- 查看日志中的 `Adhesion flow` 和串口收发记录。

### Q: 波形分析结果为空？

- 确认测量数据量是否足够。
- 确认磁场数据是否在有效范围。
- 检查是否误加载了空 CSV 或格式不匹配的 CSV。

### Q: 测量较大磁场时波形削顶？

测量 ~120mT 以上磁场时需将探头量程切换为 160mT 量程。若在 +/-100mT 处削顶，说明量程配置不匹配。切换量程后需重新执行偏置校准。

### Q: 数据比对表格中文件名字太长？

表头显示的是 CSV 文件名。可在比对前将文件重命名为更有辨识度的名称。

### Q: 打包后配置文件在哪里？

打包运行时，配置文件位于 `dist/MARS/configuration.txt`。程序会从可执行文件同级目录读取它。

---

## 附录：串口命令参考

| 命令 | 功能 | 示例 |
| --- | --- | --- |
| `B~` | 爪盘旋转 | `B~` |
| `S~` | 停止旋转/停止采集 | `S~` |
| `X{pos}~` | X 轴移动到指定位置 | `X5000~` |
| `Z{pos}~` | Z 轴移动到指定位置 | `Z3000~` |
| `N{distance}~` | Z 轴相对移动 | `N2000~` |
| `P~` | Z 轴自检/下压贴靠 | `P~` |
| `Y~` | X 轴左贴靠 | `Y~` |
| `Y-~` | X 轴右贴靠 | `Y-~` |
| `I~` | 滑台复位 | `I~` |
| `K{sec}~` | 定时采集，用于偏置校准 | `K3~` |
| `?XZ~` | 查询 X/Z 位置 | `?XZ~` |
