# 磁环检测步进电机控制系统 - 软件通信手册

## 目录
1. [系统概述](#1-系统概述)
2. [硬件连接和接口](#2-硬件连接和接口)
3. [USB通信协议](#3-usb通信协议)
4. [采集流程详解](#4-采集流程详解)
5. [输入输出格式](#5-输入输出格式)
6. [数据格式](#6-数据格式)
7. [注意事项](#7-注意事项)

---

## 1. 系统概述

### 1.1 项目简介

本系统是一个基于 STM32G474RBT6 微控制器的高精度磁环位置检测系统，运行 FreeRTOS 实时操作系统。系统通过高精度 ADC (ADS8691) 采集磁环信号，并使用 MRAM 非易失性存储器存储采集数据。

### 1.2 核心功能

- 高精度 18 位 ADC 采集
- 双轴步进电机控制 (X/Z 轴)
- 增量编码器位置反馈
- MRAM 非易失性数据存储
- USB CDC 虚拟串口通信

### 1.3 系统架构

```
┌─────────────────────────────────────────────────────────────┐
│                      FreeRTOS 任务架构                      │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  MainTask (Normal 优先级)                                    │
│  - USB 设备初始化                                           │
│  - ADS86xx 初始化                                           │
│  - 步进电机 GPIO 初始化                                      │
│  - 看门狗喂狗 (100ms)                                       │
│                                                              │
│  UsartTask (Low 优先级)                                     │
│  - 检查 cmd_ready → 解析 USB 命令                             │
│  - 检查 usb_send_rest_data_flag → 发送剩余数据              │
│                                                              │
│  GPIOTask (Low 优先级)                                       │
│  - GPIO_TASK() → 处理外部中断                               │
│  - ReadKey_Task() → 按键扫描                               │
│  - adc_task() → 光传感器 ADC 采集                          │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

---

## 2. 硬件连接和接口

### 2.1 MCU 资源分配

| 外设 | 用途 |
|------|------|
| **STM32G474RBT6** | 主控 (ARM Cortex-M4, 170MHz) |
| **TIM1** | X 轴步进电机 PWM |
| **TIM2** | Z 轴步进电机 PWM |
| **TIM3** | Y 轴步进电机 PWM |
| **TIM6** | 基本定时器 (ADC 触发) |
| **TIM8** | WS2812 LED PWM |
| **TIM15** | DRV8870 电机驱动 PWM |
| **SPI1** | ADS8691 ADC / MRAM 通信 |
| **USART1** | 串口通信 (备用) |
| **USB** | USB CDC 虚拟串口 |
| **ADC1** | 光传感器 ADC |

### 2.2 外围器件

| 器件 | 型号 | 功能 |
|------|------|------|
| **ADC** | ADS8691 | 18 位高精度模数转换 |
| **存储** | MRAM (512KB) | SPI 接口非易失存储 |
| **LED** | WS2812B | RGB 状态指示 |
| **电机驱动** | DRV8870 | 双 H 桥驱动 IC |
| **编码器** | 增量编码器 | 位置反馈 (106 脉冲/转) |
| **限位开关** | 光电传感器 | X/Z 轴原点检测 |

### 2.3 引脚连接

关键信号引脚说明：

| 信号 | 引脚 | 功能说明 |
|------|------|----------|
| ADS8691 CS | SPI1_CS | ADC 片选信号 |
| ADS8691 RVS | RVS (ADC_DATA_READY) | ADC 转换完成/数据就绪信号 |
| Encoder_A | Encoder_A_Pin | 编码器 A 相 (用于停止检测) |
| Encoder_B | Encoder_B_Pin | 编码器 B 相 (用于脉冲计数) |
| MRAM_CS | SPI_MRAM_CS | MRAM 片选信号 |
| LIGHT_X | LIGHT_X_Pin | X 轴限位开关 |
| LIGHT_Z | LIGHT_Z_Pin | Z 轴限位开关 |

---

## 3. USB通信协议

### 3.1 命令格式

所有命令必须以 `~` 结尾，格式为：

```
<命令字符>[参数]~
```

### 3.2 命令列表

| 命令 | 功能 | 示例 |
|------|------|------|
| `A` | 查询当前 ADC 值 | `A~` |
| `B` | 开始采集/位置控制 | `B~` / `B542720~` / `B-542720~` |
| `S` | 停止采集 | `S~` |
| `R` | 读取 MRAM 数据 | `R~` |
| `X<pos>` | X 轴移动到位置 | `X5000~` |
| `Z<pos>` | Z 轴移动到位置 | `Z3000~` |
| `O` | 停止所有电机 | `O~` |
| `I` | 电机位置初始化 (回原点) | `I~` |
| `P` | Z 轴自检 (光 ADC 阈值检测) | `P~` |
| `Y` | X 轴自检 (光 ADC 阈值检测) | `Y~` |
| `N<num>` | Z轴相对脉冲移动 (带符号数) | `N2000~` / `N-2000~` |
| `K<sec>` | 定时采集模式 (TIM4, 100Hz) | `K5~` 采集5秒 |
| `M` | 查询所有状态参数 | `M~` |
| `T` | 测试模式 | `T~` |
| `?X` | 查询 X 轴位置 | `?X~` |
| `?Z` | 查询 Z 轴位置 | `?Z~` |
| `?XZ` | 查询双轴位置 | `?XZ~` |

### 3.3 命令详解

#### A - 触发 ADC 采样并应答

```
发送: A~
响应: <2字节ADC值> (立即返回当前ADC采样值)
```

触发一次 ADS8691 ADC 采样并立即返回结果。

#### B - 开始采集/位置控制

B 命令支持三种格式：

1. **B~** - 原有采集模式，转动 1 圈 (360°)
   - 采集距离: `FINISH_PULSE_CNT = 1024 * 106 * 5 = 542720` 脉冲 = 360°
   - **注意**: 直流有刷电机减速比5:106，编码器在电机输入端(1024脉冲/转)，一圈=542720脉冲。使用时需做好角度与脉冲数的映射。

2. **B542720~** 或 **B+542720~** - 正转(顺时针)到指定位置
   - 参数: 目标脉冲数 (无符号)

3. **B-542720~** - 反转(逆时针)到指定位置
   - 参数: 目标脉冲数 (带负号)

#### S - 停止采集

```
发送: S~
响应: 无
```

立即停止 TIM15 PWM 输出，停止电机运转。

#### R - 读取 MRAM 数据

```
发送: R~
响应: <所有采集的ADC数据>
```

从 MRAM 中读取所有采集的数据并通过 USB 发送。

#### X/Z - 轴移动

```
发送: X5000~  (X轴移动到脉冲位置5000)
发送: Z3000~  (Z轴移动到脉冲位置3000)
响应: 无
```

系统自动判断当前位置与目标位置的关系，确定方向后启动电机。

#### I - 回零初始化

```
发送: I~
响应: "StepMotor INIT\r\n"
```

执行回零操作，将 X 轴和 Z 轴移动到限位开关位置作为机械原点。

#### P/Y - 轴自检

```
发送: P~  (Z轴自检)
发送: Y~  (X轴自检)
响应: "Self Detect Z Axis\r\n" 或 "Self Detect X Axis\r\n"
      完成后: "Z Axis Self Detect Finished\r\n" 或 "X Axis Self Detect Finished\r\n"
```

使用光传感器 ADC 阈值检测自动寻找限位位置。

#### N - Z轴相对脉冲移动

```
发送: N2000~   (Z轴向下移动2000脉冲)
发送: N-2000~  (Z轴向上移动2000脉冲)
响应: "Z Move +2000\r\n" 或 "Z Move -2000\r\n"
```

带符号的相对脉冲移动命令，正数为向下/正转，负数为向上/反转。

#### M - 查询系统状态

```
发送: M~
响应:
light_adc:xxx,xxx,xxx
Hall_adc:xxx
end_addr:xxx
Inc_PulseCnt:xxx
X_PulseCnt:xxx
Z_PulseCnt:xxx
Finishcnt:xxx
```

返回所有关键系统参数：
- light_adc: 光传感器 ADC 值,初始值,差值
- Hall_adc: 霍尔传感器(ADC)上次值
- end_addr: MRAM 地址
- Inc_PulseCnt: 增量编码器脉冲计数
- X_PulseCnt: X 轴脉冲计数
- Z_PulseCnt: Z 轴脉冲计数
- Finishcnt: 目标脉冲数

#### ? - 查询位置

```
发送: ?X~    (查询X轴)
发送: ?Z~    (查询Z轴)
发送: ?XZ~   (查询双轴)
响应: X:xxx\r\n
      Z:xxx\r\n 或 X:xxx,Z:xxx\r\n
```

---

## 4. 采集流程详解

### 4.1 完整采集流程 (B 命令)

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                         数据采集流程                                         │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  1. 命令阶段                                                                 │
│  ┌─────────┐    ┌─────────┐    ┌─────────┐    ┌─────────┐                 │
│  │ USB发送 │───►│ 命令解析 │───►│ 初始化  │───►│ 启动电机│                 │
│  │ 'B'命令 │    │ usart   │    │ MRAM清空│    │ TIM15   │                 │
│  │         │    │ decode  │    │         │    │ PWM     │                 │
│  └─────────┘    └─────────┘    └─────────┘    └─────────┘                 │
│                                                                          │
│  2. 电机驱动阶段                                                           │
│  ┌──────────────┐   ┌──────────────┐   ┌──────────────┐               │
│  │ TIM15 PWM    │──►│ DRV8870      │──►│ 步进电机     │               │
│  │ 94.4kHz      │   │ 驱动IC       │   │ 转动         │               │
│  └──────────────┘   └──────────────┘   └──────────────┘               │
│                                                                          │
│  3. 编码器计数阶段                                                         │
│  ┌──────────────┐   ┌──────────────┐   ┌──────────────┐               │
│  │ 电机带动     │──►│ Encoder_B    │──►│ EXTI9_5     │               │
│  │ 编码器       │   │ 脉冲输出     │   │ 中断         │               │
│  └──────────────┘   └──────────────┘   └──────────────┘               │
│        │                                                              │
│        ▼                                                              │
│  ┌─────────────────────────────────────────────────┐                   │
│  │ HAL_GPIO_EXTI_Callback(Encoder_B_Pin)          │                   │
│  │   • pulse_cnt++ (顺时针) 或 pulse_cnt-- (逆时针) │                   │
│  │   • 判断是否到达目标脉冲数                        │                   │
│  │   • 每2个脉冲触发1次ADC采集                     │                   │
│  └─────────────────────────────────────────────────┘                   │
│                                                                          │
│  4. ADC 采集阶段                                                          │
│  ┌──────────────┐   ┌──────────────┐   ┌──────────────┐               │
│  │ ADS8691_CS   │──►│ RVS (数据就绪)│──►│ SPI DMA     │               │
│  │ 片选         │   │ 下降沿        │   │ 接收         │               │
│  └──────────────┘   └──────────────┘   └──────────────┘               │
│        │                                                              │
│        ▼                                                              │
│  ┌─────────────────────────────────────────────────┐                   │
│  │ HAL_GPIO_EXTI_Callback(RVS_Pin)                │                   │
│  │   • 判断 CS=0 且 RVS=0                          │                   │
│  │   • 启动 SPI DMA 接收                           │                   │
│  └─────────────────────────────────────────────────┘                   │
│        │                                                              │
│        ▼                                                              │
│  ┌─────────────────────────────────────────────────┐                   │
│  │ HAL_SPI_RxCpltCallback()                       │                   │
│  │   • 解析 18 位 ADC 值                           │                   │
│  │   • 存入 USB TX Buffer                         │                   │
│  │   • 存入 MRAM                                  │                   │
│  │   • LED 闪烁指示                               │                   │
│  └─────────────────────────────────────────────────┘                   │
│                                                                          │
│  5. 数据发送阶段                                                          │
│  ┌──────────────┐   ┌──────────────┐                                   │
│  │ 到达目标     │──►│ USB发送      │                                   │
│  │ 脉冲数       │   │ 剩余数据     │                                   │
│  └──────────────┘   └──────────────┘                                   │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 4.2 关键中断处理

#### HAL_GPIO_EXTI_Callback

```c
void HAL_GPIO_EXTI_Callback(uint16_t GPIO_Pin)
{
    switch(GPIO_Pin)
    {
        case Encoder_B_Pin:
            // 编码器 B 相中断处理
            if(HAL_GPIO_ReadPin(Encoder_A_GPIO_Port, Encoder_A_Pin) == GPIO_PIN_RESET)
            {
                if(dc_motor_direction == 0)  // 顺时针
                {
                    IncEncoder.pulse_cnt++;
                    if(IncEncoder.pulse_cnt >= IncEncoder.finish_pulse_cnt && motor_start_flag == 1)
                    {
                        usb_send_rest_data_flag = 1;
                        Motor_Stop();
                        motor_start_flag = 0;
                    }
                }
                else  // 逆时针
                {
                    IncEncoder.pulse_cnt--;
                    if(IncEncoder.pulse_cnt <= IncEncoder.finish_pulse_cnt && motor_start_flag == 1)
                    {
                        usb_send_rest_data_flag = 1;
                        Motor_Stop();
                        motor_start_flag = 0;
                    }
                }
                // 每隔2个脉冲触发一次 ADC 采集
                if(motor_start_flag == 1 && IncEncoder.pulse_cnt % 2 == 0)
                {
                    ADS8691_CS_L;  // 片选 ADS8691
                }
            }
            break;

        case RVS_Pin:
            // ADC 数据就绪中断
            if(HAL_GPIO_ReadPin(RVS_PIN_PORT, RVS_PIN_PIN) == GPIO_PIN_RESET
               && HAL_GPIO_ReadPin(SPI1_CS_GPIO_Port, SPI1_CS_Pin) == GPIO_PIN_RESET)
            {
                HAL_SPI_Receive_DMA(&hspi1, ads8691.rx_buf, 3);
            }
            break;
    }
}
```

#### HAL_SPI_RxCpltCallback

```c
void HAL_SPI_RxCpltCallback(SPI_HandleTypeDef *hspi)
{
    if(hspi == &hspi1)
    {
        // 释放 SPI 片选
        HAL_GPIO_WritePin(SPI1_CS_GPIO_Port, SPI1_CS_Pin, GPIO_PIN_SET);

        // 解析 18 位 ADC 值
        ads8691.value = (ads8691.rx_buf[0] << 10) | (ads8691.rx_buf[1] << 2) | (ads8691.rx_buf[2] >> 6);
        value_u16 = ads8691.value >> 2;

        // 存入 USB 发送缓冲区
        temp[0] = value_u16 >> 8;
        temp[1] = value_u16;
        fill_2B_in_usb_tx_buffer(temp);

        // MRAM 地址递增
        w_mram.addr += 2;

        // LED 闪烁指示
        if(check != 0)
        {
            HAL_GPIO_TogglePin(LED_GPIO_Port, LED_Pin);
        }
    }
}
```

### 4.3 USB 数据发送流程

USB 数据发送采用双缓冲机制：

- **缓冲区大小**: `USB_TX_BUF_NUM * USB_TX_BUF_LEN = 2 * 1350 = 2700` 字节
- **单缓冲区大小**: `1350` 字节 (675 * 2 字节)

```
数据流程:
1. fill_2B_in_usb_tx_buffer() 将 2 字节数据存入缓冲区
2. 当缓冲区满 (1350 字节) 时自动触发 USB 发送
3. 采集完成后调用 send_rest_data_in_usb_tx_buffer() 发送剩余数据
```

---

## 5. 输入输出格式

### 5.1 命令输入格式

USB 命令通过 CDC 虚拟串口发送，格式如下：

| 类型 | 格式 | 说明 |
|------|------|------|
| 无参数命令 | `<CMD>~` | 命令字符 + 结束符 |
| 带数字参数 | `<CMD><number>~` | 命令 + 数字参数 + 结束符 |
| 带符号参数 | `<CMD><+/-><number>~` | 命令 + 符号 + 数字 + 结束符 |

### 5.2 响应输出格式

| 命令 | 响应格式 | 示例 |
|------|----------|------|
| A | `<2字节ADC值>` | `0x12 0x34` |
| M | 多行文本 | `light_adc:100,200,-100\r\n...` |
| ?X | `X:<数字>\r\n` | `X:5000\r\n` |
| ?Z | `Z:<数字>\r\n` | `Z:3000\r\n` |
| ?XZ | `X:<数字>,Z:<数字>\r\n` | `X:5000,Z:3000\r\n` |
| R | 原始二进制数据 | 连续 2 字节/包的 ADC 数据 |

### 5.3 文本响应

系统状态文本响应使用 ASCII 格式：

```
light_adc:100,200,-100\r\n
Hall_adc:12345\r\n
end_addr:54000\r\n
Inc_PulseCnt:538240\r\n
X_PulseCnt:5000\r\n
Z_PulseCnt:3000\r\n
Finishcnt:538240\r\n
```

---

## 6. 数据格式

### 6.1 ADC 数据格式

#### ADS8691 ADC 数据

- **分辨率**: 18 位
- **数据存储**: 16 位有效数据 (高 2 位为符号位扩展)
- **SPI 通信**: 3 字节 (24 bit) 帧，实际有效数据 18 位

```
SPI 接收缓冲区 (ads8691.rx_buf):
┌─────────┬─────────┬─────────┐
│ rx_buf[0]│ rx_buf[1]│ rx_buf[2]│
│ (8 bits) │ (8 bits) │ (8 bits) │
└─────────┴─────────┴─────────┘
     │        │        │
     ▼        ▼        ▼
┌─────────────────────────┐
│ value = (rx[0] << 10)  │
│       | (rx[1] << 2)    │
│       | (rx[2] >> 6)    │
│ (18 位有效数据)          │
└─────────────────────────┘
```

#### USB 传输格式

```
每包 ADC 数据: 2 字节 (高字节在前)
┌─────────┬─────────┐
│  Byte0  │  Byte1  │
│  高8位  │  低8位  │
└─────────┴─────────┘

value_u16 = value >> 2 (取 16 位有效值)
```

### 6.2 MRAM 数据格式

#### 存储容量

- **MRAM 大小**: 512KB = 524288 字节
- **采样容量**: 约 170,666 次采样 (每采样 3 字节)

#### 存储格式

```
MRAM 地址:  0     2     4     6     8     ...
            ┌─────┬─────┬─────┬─────┬─────┐
数据:        │ ADC │ ADC │ ADC │ ADC │ ADC │
(每2字节)   │[Hi][Lo]│[Hi][Lo]│[Hi][Lo]│[Hi][Lo]│
            └─────┴─────┴─────┴─────┴─────┘

注: MRAM 实际存储使用 3 字节/采样 (18 位原始值)
    USB 发送时转换为 2 字节/采样 (16 位有效值)
```

### 6.3 位置数据格式

#### 编码器脉冲计数

- **类型**: 32 位有符号整数 (`int32_t`)
- **方向定义**:
  - 顺时针 (CW): 脉冲数递增
  - 逆时针 (CCW): 脉冲数递减

```
IncEncoder.pulse_cnt (int32_t)
范围: -2,147,483,648 ~ 2,147,483,647
```

#### 步进电机脉冲

```
MDR_Step_Motor.motor[id].pulse_cnt (int)
MDR_Step_Motor.motor[id].target_pulse_cnt (int)
```

### 6.4 USB 缓冲区定义

```c
// usbd_cdc_if.c
#define USB_TX_BUF_NUM  2       // 双缓冲
#define USB_TX_BUF_BYTE 2        // 每采样 2 字节
#define USB_TX_BUF_LEN  (675 * USB_TX_BUF_BYTE)  // 1350 字节/缓冲

static uint8_t usb_tx_buffer[USB_TX_BUF_NUM * USB_TX_BUF_LEN];  // 总 2700 字节

static uint16_t usb_tx_buf_wr_index;  // 写索引
```

---

## 7. 注意事项

### 7.1 通信注意事项

1. **命令结束符**: 所有命令必须以 `~` 结尾，否则命令不会被解析
2. **USB 枚举**: 首次连接需要等待 USB 设备枚举完成
3. **波特率**: USB CDC 为虚拟串口，无需配置波特率
4. **缓冲区大小**: USB 发送缓冲区为 2700 字节，采集大量数据时注意分批发送

### 7.2 采集注意事项

1. **采集触发**: 每 2 个编码器脉冲触发 1 次 ADC 采集
2. **采集距离**: 默认 `FINISH_PULSE_CNT = 542720` 脉冲 (1圈=360°)
   - **直流有刷电机参数**: 减速比5:106，编码器在电机输入端(1024脉冲/转)
   - 一圈脉冲数 = 5 × 106 × 1024 = 542,720 脉冲 = 360°
   - 使用时需做好角度与脉冲数的映射
3. **MRAM 容量**: 最大约 170,666 次采样，超出后 MRAM 地址会溢出
4. **数据丢失**: 缓冲区满时会自动发送，但高频率采集时注意 USB 带宽

### 7.3 电机控制注意事项

1. **限位保护**: 逆时针方向移动时会检查限位开关
2. **行程限制**:
   - X 轴: 最大 `STEP_MOTOR_X_MAX_PULSE_CNT = 5500 * 8 = 44000` 脉冲
   - Z 轴: 最大 `STEP_MOTOR_Z_MAX_PULSE_CNT = 10200 * 8 = 81600` 脉冲
3. **回零操作**: 建议首次使用前执行 `I~` 命令回零
4. **急停**: 可随时发送 `S~` 停止采集，`O~` 停止所有电机

### 7.4 故障排查

| 问题 | 可能原因 | 解决方案 |
|------|----------|----------|
| 电机不转动 | DRV8870 电源/限位触发 | 检查电源/限位开关 |
| ADC 无数据 | ADS8691 通信/RVS 信号 | 检查 SPI 连接 |
| USB 通信失败 | 设备未枚举 | 重新插拔 USB |
| MRAM 写入失败 | SPI 片选/型号错误 | 检查硬件连接 |

### 7.5 版本信息

- **固件版本**: V3.5
- **发布日期**: 2026-03-10
- **更新内容**: DC电机支持正反转控制，B指令支持带符号目标位置；新增K指令定时采集模式(TIM4，100Hz)

---

## 附录：关键函数索引

| 函数名 | 文件位置 | 功能 |
|--------|----------|------|
| `usart_decode()` | mygpio.c | USB 命令解析 |
| `HAL_GPIO_EXTI_Callback()` | mygpio.c | GPIO 中断处理 |
| `HAL_SPI_RxCpltCallback()` | mygpio.c | SPI 接收完成回调 |
| `Motor_Start_Direction()` | mygpio.c | 带方向电机启动 |
| `MRAM_MultiRead()` | mygpio.c | MRAM 批量读取 |
| `fill_2B_in_usb_tx_buffer()` | usbd_cdc_if.c | USB 缓冲区写入 |

---

**文档版本**: 1.0
**创建日期**: 2026-03-10
**作者**: CreaCoder
